version: '3.8'

services:
  api:
    build: .
    container_name: legendarium-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    networks:
      - legendarium-network

networks:
  legendarium-network:
    driver: bridge
